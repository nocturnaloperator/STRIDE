import 'package:flutter/material.dart';
 
/// Placeholder for the activity feed tab. Once you're ready, this will
/// follow the same pattern as ProfileScreen: watch a `homeFeedProvider`
/// backed by its own repository.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});
 
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Home feed — coming soon')),
    );
  }
}