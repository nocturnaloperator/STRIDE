import 'package:flutter/material.dart';
 
/// Placeholder for the activity tracking / history tab (the GPS map
/// screen you mentioned wanting to build after this).
class ActivitiesScreen extends StatelessWidget {
  const ActivitiesScreen({super.key});
 
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Activity tracking — coming soon')),
    );
  }
}